# 全要求のカタログ再現・追調査・品質改善

状態: 実行中。起点: 要求整理 PR #173、131要求・282 AC、試作03段13。

## POの目的

全要求をカタログで再現し、リサーチの取りこぼしを再確認して要求化する。取捨選択が十分容易になるまでカタログを整理し、現状を超えるデザイン品質を試す。必要な実在サイトの追調査も行う。既存の未検証を完了へ読み替えず、途中の使いやすさ改善だけでこの目的を完了にしない。

## 完了を示す証拠

1. 全要求・ACに、再現ページまたは操作デモ、設定と状態、再現手順、実測証跡、制約が対応する。表示以外の要求は操作・異常系・契約のデモと実行結果を持たせる。単なる説明カードや関連画像を再現完了と数えない。
2. 9系統を上限にせず、既存の種別台帳と実在例を再点検する。抜けを出所・目的・状態・採否・要求ID・検証対象へ接続する。調査件数だけで網羅としない。
3. 共通部品・各ページ面・状態を用途で探し、PC/SPを確認し、候補を比較し、採用候補・保留・除外と理由を保存・持ち出せる。これらの操作を実ブラウザで検証する。
4. 有料記事・インタビュー・BLPの独立した意味と管理、外部機能との接続・権限別表示・遷移を再現する。決済・会員等の業務処理をテーマへ持ち込まない。
5. デザイン改善は同じ内容・画面幅の変更前後で評価する。階層、組版、余白、画像、導線、アクセシビリティ、速度、JS無効・失敗時の成立を証跡に残す。カタログの外枠だけの改善でテーマ品質改善を代替しない。
6. 全要求と追加要求のカタログ再現、操作テスト、品質比較、公開情報検査、CIを確認する。G3や外部本番反映の承認は別の判断として扱う。

## 実行順

| 段階 | 作業 | 状態 |
| --- | --- | --- |
| 1 | 全要求の再現台帳、既存画像の比較・選択UI、現在の空白を可視化 | 比較・選択・保存とAC ID/残件検索・確認状況絞り込みを検証。全ACの証拠対応は継続 |
| 2 | 追調査の差分を要求化。有料記事・インタビュー・BLPの代表再現 | 3 AC追加、独立WP代表再現を検証。外部接続・専用編集UI・全継承は残る |
| 3 | 9系統の残り、5継承セットの例、検索・規約・状態遷移・外部機能接続 | 学習・ヘルプ・用語集・階層/前後/検索と範囲外/公開状態変更からの復帰を追加。常設案内/規約11面とJSON設定・受付引渡し例を追加。会社案内パターンの実UI挿入と11種類の切替・保存・再読込・公開反映42検査を確認。6面の共通/独自/非表示を既存パーツへ接続し保存設定を検証。5セット拡張、全継承、実サービス接続、残り状態は未完了 |
| 4 | 非表示要求の操作・失敗・契約デモと実機証跡。全ACとの対応 | 掲載取り消し/人物参照破損/明示再公開の73検査を追加。全件対応は未完了 |
| 5 | デザイン案の比較と改善、アクセシビリティ・速度の改善検証 | 常設11面の操作領域を共通44pxへ改善。265検査と会社案内/料金PC/SPの変更前後比較を追加。全面の美観比較・速度は未完了 |
| 6 | 追加要求を含めた全件監査、比較・保存・再開の操作検証、GitHub同期 | 未完了 |

## 最初の確認

- 試作には978枚の撮影索引と用語集があるが、リポジトリ内に選択メモ・比較・全要求の再現状況を統合するビューがない。
- 131要求への画像の関連付けは発見用の手掛かりであり、受入条件の達成証拠ではない。
- 追調査の入口: 会員記事の公開プレビューと権限ごとの閲覧、入力確認から修正への戻り方、WordPressのパターン登録契約。原文や非公開対応表は複製せず、公開可能な観察と要求への対応を記録する。

作業ブランチ: `codex/catalog-completion-20260908`。文書整理のブランチから継続し、PR #173とは別の作業として管理する。

## 共通ナビ接続の進捗

共有ヘッダーを標準wp_navigation参照へ接続。62検査とseed再実行の保持確認、共通パーツ428検査・36画像の更新、有料/人物/BLP等191検査、学習252検査、常設397検査・操作領域265検査の回帰を確認。PARTS-02A/Bは部分確認であり、nativeの固定ナビ、全型の操作、独立CTA、管理UI、残りの全要求再現は継続する。

## サイト全体検索の再確認

学習検索とは別に、サイト検索の専用結果面がない不足を実測（30検査中18失敗）。標準ブロックの結果・ゼロ件・再検索を追加し73検査で確認。3状態6画像を選択カタログへ追加。公開状態/権限行列、ページ送り異常系、未入力専用状態、絞込、正式IR候補化は[検索計画](../research/2026-09-08-site-search/PLAN.md)で継続する。

## WT-EVT-0310: 検索要求と境界の具体化

WT-FR-SEARCH-01・4 AC・WT-AT-SEARCH-01を追加（133要求・294 AC）。カテゴリのページ送り非表示が検索にも作用する不具合を修正。範囲外ページで0件と誤表示する不具合を実測し、検索主クエリを有効ページへ補正する。公開状態変更後の復帰と本文非混入を専用fixtureで検証する。全権限・不正入力・日本語/キーボード・追加クエリ性能と第三者検索拡張の共存は残る。

検索の不正入力/キーボード297検査、キーボードページ送りを含む境界62検査、日本語locale50検査を追加。SEARCH-01Dを代表PoC確認済みへ対応付けた。空欄専用状態・全権限・不正ページ指定の有効結果表示・性能/第三者検索拡張は継続する。

不正ページ指定でも有効な結果を表示することを追加照合し、SEARCH-01CもPoC範囲の確認済みへ更新。全体は12確認済み・13部分確認・269証拠未対応。全体完了ではない。

検索未入力時の結果一覧を専用案内へ置換し、通常のゼロ件と分離した。検索対象の説明と主見出しの階層を改善。空白を含む105検査と、日本語74検査を追加し、PC/SPの変更前後比較をカタログへ接続する。全権限行列・全体の品質改善は継続。

SEARCH-01Aを未入力/ゼロ件の区別を含む105検査でPoC確認済みへ更新。全294条件のうち13確認済み・12部分確認・269証拠未対応。全要求再現と全体の品質改善は未完了。

ログイン状態の検索335検査で、パスワード保護本文への検索一致が記事名/件数を返す不足を84失敗として再現。購入本文の非混入と直接閲覧の権限差は成功。SEARCH-01Bの既知の残件をカタログへ反映した。パスワード解除の有無と検索対象/件数の一致を次の修正対象とし、全体の確認済み件数は増やさない。

検索の未解除パスワード記事を、結果と件数補正の共通条件で除外する修正を追加。ログイン行列335件は全成功。匿名も含めた解除・誤入力・解除取り消し・パスワード変更・非公開除外・管理/副クエリ境界319件を確認した。既存検索/入力/公開境界105/297/62件、共通パーツ428件、有料等191件、学習252件、常設397件も成功。全294条件の確認済み13・部分確認12・未対応269は維持し、全権限と実サービス接続・性能は継続する。

カタログの選別再開を改善。比較候補/面/目的/採用状態/PC-SP/検索語/要求タブ/関連候補/展開件数を復元し、理由で検索できる。比較候補の個別解除と詳細編集後の操作位置を修正。変更前4不足を再現し、最終15操作検査とPC/SP画像を保存。全要求の再現、全面のデザイン比較・品質改善、ブラウザをまたぐ検討状態の移行は継続する。

比較画像の読取りを改善。全体/幅合わせ/原寸を詳細・比較へ追加し、候補数に合わせて列幅を割り当てる。原寸幅、縦横のキー操作、表示復元、選択メモ保持を含む17操作検査が成功。PC/SP画像を保存し、全要求の再現件数を増やさず比較操作の改善として管理する。

フォームの不正段階を完了として受理する不足を修正。問い合わせの確認3方式×PC/SP×JS有無、修正時の値保持、nonce/不正段階の拒否、エラーからの再開を218検査で確認。FORM-01A/Bを部分確認へ対応付け、全体は13確認済み・14部分確認・267未対応。全種別/全項目、LP/イベント配置、期限・残席によるイベント受付遷移は継続する。

### フォーム全種別の正常経路と共有カタログの照合

9種別＋全項目セットの40条件を実機操作し282検査成功。FORM-01A/Bは部分確認のまま証拠範囲を拡大。異常値境界・段階式・別ページ完了・LP/event配置は残件。共有されたClaudeカタログは外部取得403。ローカル生成HTML956枚を現行カタログの画像名と照合し、欠落0件。履歴の最終報告978枚とは版が異なるため共有先最新版の確認とは扱わない。比較記録はselection-catalog/reference-catalog-comparison.json。

### フォームの段階式とLP/イベント配置

段階式の9種別＋全項目セットを40条件で操作し395検査成功。LP/イベントの既定非表示・選択時項目・確認修正・別ページ完了を83検査で照合。イベントは問い合わせ指定より参加申込の文脈が優先され完了ページにも継承する。FORM-01A/Bは部分確認のまま証拠を拡大。入力型の異常境界・全配置の視覚/通信・全テンプレート解決境界・サーバー非保存/非送信の全経路は残件。

### フォームの異常値受理を実機で検出

全項目セットへ直接POSTし9種類の不正値受理（94検査中36失敗）を再現。PHPの暦日・希望日・整数・URL・同意値を検証しJS側も対応。正当な閏日・年月・通常値を受け付ける対照も置く。境界verifierは112検査成功、既存4経路の回帰検証を実行する。FORM-01A/Bの部分確認を維持し、配列の深さ、国際化URL、全配置/外部通信等は未完了とする。

### イベント時刻・残席の未接続を基準測定

既存EVENT-01A/Bに従い、専用labの一時ページに開始前/開始境界/満席/空席復帰/締切前/締切境界の6fixtureを配置。現行のevent_statusクエリを使わず検査し13検査中6不一致。時刻/申込数の試験メタは現行未接続であり、受付中との偶然一致を遷移成功に算入しない。event-state/PLAN.mdとbaseline.jsonを保存。次段はこの入力を読む表示経路と受付不能時の導線、境界・無効値・タイムゾーンの検証。正式な入力契約や業務申込処理の確定とは扱わない。

### イベント受付fixtureの表示・拒否・導線を接続

イベント121検査と既存フォーム1090検査が成功。受付不能時はフォームを描画せず直接POSTを拒否し、状態案内へのリンクは維持する。4状態8画像をカタログへ追加し611候補/1094画像。EVENT-01A/Bを部分確認に追加、確認13・部分16・未対応265。Claudeの通知はPRとWIP差分について投入済み、レビュー受領/完了は未確認。未検証の無効時刻・全配置・地図/サイドバー・業務予約は残件。

### イベントの無効時差と定員境界

17入力の341検査で、不正な時差+99:00/+09:99を正規化して受理する不足（32失敗）を再現。RFC 3339の時・分範囲で拒否するよう修正し341検査成功。正当な時差表記、期間逆転、型・負値・定員0/超過も照合。通常イベント121検査成功、既存フォームの回帰検証を継続する。地域タイムゾーン/DST/閏秒/取消・全配置は未完了。

境界修正の再検証完了: 不正offsetを拒否し、イベント境界341・状態121・既存フォーム1090の計1552検査が成功。全7証跡のsource digestを現行ファイルと照合し、FORM/EVENTの受入条件参照を更新した。地域TZ/DST・全デザイン軸・実予約は引き続き未完了。

再起動前の停止点（PO指示）: 今回の日時境界修正と証拠更新で区切る。カタログE2Eは初回17/18で、dialog close時の保存と異常workspace注入のテスト競合を検出。close handlerのfocus復帰を待ってから注入するよう修正し、再実行18/18成功。npm test（要求整合性・consumer health）成功、PHP構文検査成功。カタログは611候補・1094画像、ACは13 PoC確認済み / 16部分 / 265証拠未対応 / 0 stale。全要求完遂ではない。
再開時は current HEAD / CI / Claude receipt を再取得し、通知のARMEDを受領と混同しない。統合層の受信hook修正は別コミット91ba69a。未完了範囲は acceptance-audit.json の各 remaining と本PLANを継承し、新しい実装へ進む前に現物を確認する。統合層のtheme pinは変更しない。

### 2026-09-09 再開・フッターデータ不足

PHP検査環境を導入しunit/security 201テスト・492 assertions成功、既存PHPCS57ファイルの指摘0、試作PHP66ファイル構文成功。PR #174 current HEAD c87fc8eの通知にDELIVERED記録が付き、POからClaude受信報告も得た。レビュー完了は未確認。
フッターの固定サンプルと空データ省略の不足を調査。専用labのGETによる16検査中8失敗を記録し、`docs/research/2026-09-09-footer-data/PLAN.md` にデータ保存・公開DOM・空枠省略・カタログ比較の次工程を記した。WT-AC-PARTS-01Cは未対応のまま保持する。

フッター保存元の方式比較: 標準Navigationの空枠残存とdraft/private参照の公開メニューfallbackを実機確認（11検査中6失敗）。公開参照・空出力を検査するfooter-navigation.php候補関数では同系列11成功。フッター本体への組込みとSite Editor/PCSP検証は次工程で、AC昇格はまだ行わない。

フッター入力境界: 空白だけの表示名で無名リンクとnav枠が残る不足を20検査中2失敗として再現し、描画中だけ空名を省略するfilterで修正。20検査成功。サイト編集画面とフッター本体への組込みは残件。

フッター本体に標準Navigationパターンを接続し、保存モデルwp_template_part/wp_navigationから公開する50検査成功。PC/SP×JS有無、空→有→空、off、折り畳みを照合。共通変更に伴う既存29 ACはstaleへ戻しカタログへ表示。継承から回帰を取り直している。Site Editor実操作とカタログ追加は未完了。

### 2026-09-09 カタログ比較の判断情報強化

ビジュアル実装はAstra（effort low）が担当し、Codexは独立検収と回帰テスト追加だけを行った。候補カードへ撮影幅・関連要求数・保存理由の抜粋を出し、比較ダイアログへ目的・撮影・状態・理由・関連要求の表を追加した。値が異なる行は色と「差分あり」の文字で示し、比較内の状態・理由編集を即時反映する。関連要求は受入達成の証拠ではないことを明記した。

Codex検収ではカタログE2E 21/21、npm test、PC 1440px / SP 375pxの目視、文書横溢れなし、比較表のcaptionとrow/column scope、キーボードfocus、状態・理由の即時反映を確認した。スクリーンリーダー実機と選別時間のユーザーテストは未実施。候補数613・画像1098・要求133・AC294、確認19・部分17・未対応258・stale 0は変えない。

### 2026-09-09 公開安全性ゲートの負例実測

`WT-NFR-CRED-01` を合成fixtureだけで検証する再現スクリプトを追加した。通常ソースとprivate mapping付きresearch差分の正例2件を受理し、秘密鍵形式、既知token形式、credential代入、個人絶対path、affiliate/click tracking URL、mappingなしresearch、custom private mapping一致の負例7件を各exit 1で拒否した。実credential・実非公開名は使わず、fixtureは検査後に削除する。

正負9行を受入証拠へ束縛し、`WT-AC-NFR-CRED-01A/B` をPoC確認済みへ更新。package script変更で一時staleになったAI境界2 ACも証拠を再生成して同revisionへ再束縛した。監査は確認21・部分17・未対応256・stale 0。履歴全体、画像内文字、難読化された秘密、一般形で判別不能な固有名はこの静的差分ゲートの検証範囲外。
