# 旧要求（情報源）

旧 AGENT NEO 時代の L1 要求本体と NSRM 検証記録。PO 前提（2026-09-02）により旧工程は拘束ではなく、
`REQ-F-*` / `REQ-NF-*` / `ACC-*` は現行 L1 への継承根拠（`docs/requirements/l1/functional.md` 末尾）としてのみ参照する。
旧 gate / freeze 表示・価格・単体販売前提（ADR-024 で失効）は現行要求に含めない。
