# デザイン試作03 — 現在の進行（2026-09-08）

位置づけは L2 の画面プロト往復。要求候補・承認・本実装は [最新整理](../../requirements/current-alignment.md) で区別する。最新入力 WT-EVT-0303、整理 WT-EVT-0304、コード・証跡の基準は main e76e7c7（PR #172）。9系統を上限にしない（0299 / 0300）。

| 段 | 内容 | 現在 |
| --- | --- | --- |
| 1〜4 | 記事・404・カテゴリ・footer・LP、観察の再取得 | 試作・検査済み。README §2.1〜2.13 |
| 5 | PO反応による記事内語彙・共通部品・画像効果・LPパーツ・HOME / イベント追加 | 試作・検査済み。README §2.14〜2.25。比較媒体だけに要求範囲を限定しない |
| 6〜9 | カテゴリ強化、地図、HOME / イベント拡張、再利用固定ページパーツ、既定改定 | 試作・検査済み。README §2.26〜2.29 |
| 10 / 10b / 10c | サイドバー / ナビ、PC・SP既定、面別の継承 / 独自 / 非表示 | 試作・検査済み。README §2.30〜2.32 |
| 11 | フォーム面と入力→確認→完了 / 失敗 | formFace 160行。実送信・保存なし。README §2.33 |
| 12 | ヘッダー・フッター・固定CTAの面別設定 | chromeOwner 176行。README §2.34 |
| 13 | LP / イベントへフォームブロックを選択肢として追加 | formSlots 23行。既存型維持。README §2.35 |
| 次: B | 有料記事・インタビュー・BLPの固有部分の代表モック | 未着手。独立管理・状態・導線を早期確認（0299 / 0303） |
| 後続: C / D | 残り種別、学習・支援系、外部機能の接続・権限別表示・遷移 | 未検証を種別台帳で保持。決済・会員等の業務処理はテーマ外 |

最新の保存済み検査は90 pass / 0 fail / skippedなし、カタログ978枚。今回の整理では実機を再実行していない。Aの一巡はprivacy面、5継承セットの全例、共通認可・同意の完成を意味しない。正式判断はG3 / 開発スタイルの2件を保留し、要解決の要求境界3件をIRへ記録済み。

このPLANは試作の文書であり、HELIX DBのproject PLAN登録を意味しない。runtimeはcompletion blockedを維持している（最新整理の引継ぎを参照）。

## 9月5日の開始計画（履歴）

以下の「本PR」「次はPO反応待ち」などは当時の記録であり、現在の進行は上表を参照する。

# デザイン試作 03 — 比較媒体 × 記事・カテゴリ・404・LP（フロント先行）

- 開始: 2026-09-05。PO 決定「フロント先行で進める」（HP はフロントから作る。基盤は AGENT NEO 資産があるので大幅には困らない）。
- 位置づけ: L2 の画面プロト往復。要求・設計の決定ではない。デザイン系要件（LOOK / PARTS / VOCAB 表示型 / ZONE 積層 / SP 面 / META アイキャッチ / TPL / RECO 表示型 / BANNER 枠 / 4 軸 / 自動コントラスト）は本試作への PO 反応で「この面にこの型がこの guard 付きで出る」形の受入条件へ書き直す。非デザイン系要件は specified 相当で待たせ、凍結は急がない（iteration 2 の問い WT-Q-G3-01 / WT-Q-STYLE-01 は保留）。
- 用途: 比較・アフィリエイト媒体（台帳 `../2026-09-05-parts-pattern-taxonomy/by-purpose.md` の観察が最も厚い）。面: 記事 → 404 → カテゴリ ミニ HOME → LP。
- 入力: 台帳の型（用途別）、証跡ルール集の既定値 P01〜P33、パーツ一覧の #122 / #126 / #127 / #130 / #132 / #134、試作 02 のテーマ copy（土台）。
- 見せ方: `results/` の SP / PC キャプチャを `CATALOG-INDEX.json` からカタログにし、パーツごとに PO が型を選ぶ。
- 基盤の継ぎ目（試作中は仮置き、試作の結果から逆算して確定）: トークン構造 / slot・ゾーンの登録制 / 選択の置き場（設定 JSON・投稿メタ・block style）/ guard の差し込み口。
- 並行: 台帳の観察不足（footer・記事末尾・カテゴリ面）をブラウザで再取得中。結果はカテゴリ面・footer の型選定に使う。

## 進行

| 段 | 内容 | 状態 |
| --- | --- | --- |
| 1 | 記事面 + 404（ヘッダー制御・アイキャッチ 5・目次 4・h2 6・囲み 7・CTA 4・末尾関連 4・4 軸・自動コントラスト・選べる 404 + CV 導線） | 完了（PR #139） |
| 2 | 再取得（footer・記事末尾・カテゴリ面） | 完了（PR #140、台帳 recapture-v2） |
| 3 | カテゴリ ミニ HOME・footer 枠・記事末尾 slot | 完了（PR #141） |
| 4 | LP | 本PR |
| 5 | カタログ → PO 反応 → 問いへ還元 → デザイン系要件の受入条件を書き直す | 段 1 以降、面ごとに。反応 1 回目 受領（2026-09-05、比較表 SP・価格文字サイズ・ヘッダー PC・幅定義の4点）→ 是正 本PR。同日中に反応 2〜5 回目 受領（h2 アイコン被り・h3 番号サイズ/縦積み・見出しバリエーション、下線位置、囲みバリエーション、CTAバリエーション+PR重複抑止）→ 是正 同PRへ合流。反応 6・7 回目（比較表・pros-cons・レビューバー・ブログカード・PR・detext・contrast-guard・relatedの追加・再設計）受領 → 本ブランチ（`research/2026-09-05-design-prototype-03-reaction2`、README §2.17）でClaude案として実装・実機検証済み（verify 57/57 pass、追加撮影 76 枚、CATALOG 349→425）。次は PO 反応待ち |
