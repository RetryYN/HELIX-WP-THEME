export const scenarios=[{id:'post',label:'投稿の階層',lead:'記事から、ひとつ上の読みものへ。'},{id:'page',label:'固定ページの階層',lead:'長いページ名でも、いまの位置を見失わない。'},{id:'lp',label:'LPの典型導線',lead:'URLの深さではなく、利用者の導線を示す。'}];
export const fixture={origin:'https://example.com',nodes:{home:{name:'ホーム',path:'/',parent:null},journal:{name:'読みもの',path:'/journal/',parent:'home'},workspace:{name:'机と暮らし',path:'/journal/workspace/',parent:'journal'},post:{name:'小さな机を心地よく整える',path:'/journal/small-desk/',parent:'workspace'},guide:{name:'ご利用ガイド',path:'/guide/',parent:'home'},support:{name:'はじめての方へ',path:'/guide/start/',parent:'guide'},page:{name:'準備から利用後まで安心して進めるためのご案内',path:'/guide/start/steps/',parent:'support'},services:{name:'サービス',path:'/services/',parent:'home'},consult:{name:'暮らしの相談',path:'/services/consult/',parent:'services'},lp:{name:'机まわりの相談会',path:'/desk-session/',parent:'consult'}}};
export const copy=value=>structuredClone(value);
export function trail(source,id){const seen=new Set(),items=[];let next=id;while(next!==null){if(seen.has(next))throw Error('階層が循環しています');seen.add(next);const n=source.nodes[next];if(!n)throw Error('参照先がありません');if(typeof n.name!=='string'||!n.name.trim())throw Error('表示名が必要です');if(typeof n.path!=='string'||!/^\/(?!\/)/.test(n.path)||/[?#\\]/.test(n.path))throw Error('安全なサイト内パスが必要です');const url=new URL(n.path,source.origin);if(url.origin!==source.origin)throw Error('サイト外URLは使えません');items.unshift({id:next,name:n.name,url:url.href});next=n.parent;}if(items.length<2)throw Error('2階層以上が必要です');return items.map((x,i)=>({...x,position:i+1,current:i===items.length-1}));}
export function schema(items){return{'@context':'https://schema.org','@type':'BreadcrumbList',itemListElement:items.map(x=>({'@type':'ListItem',position:x.position,name:x.name,item:x.url}))};}
export const esc=v=>String(v).replaceAll('&','&amp;').replaceAll('<','&lt;').replaceAll('>','&gt;').replaceAll('"','&quot;');
export function breadcrumbs(items){return `<nav aria-label="パンくず"><ol>${items.map(x=>`<li>${x.position>1?'<span class="separator" aria-hidden="true">/</span>':''}<a href="${esc(x.url)}" ${x.current?'aria-current="page"':''}>${esc(x.name)}</a></li>`).join('')}</ol></nav>`;}
export function projection(source,id){const items=trail(source,id);return{items,html:breadcrumbs(items),json:schema(items)};}
