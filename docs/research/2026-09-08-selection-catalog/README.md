# 比較・選択カタログ（実行中）

試作03の1008枚を568候補へまとめ、共通部品・面・目的・検索語から探す。PC/SPの記録を切り替え、最大3候補を比較し、採用候補・保留・除外と理由をブラウザへ保存できる。JSON書出し・読込で移行する。同じ候補のメモは読込内容で更新する。正式な要求承認・G3判断にはならない。

## 開く・検証する

リポジトリのルートで実行する。

```sh
node scripts/build-selection-catalog.mjs
python3 -m http.server 8099 --bind 127.0.0.1
```

ブラウザで `http://127.0.0.1:8099/docs/research/2026-09-08-selection-catalog/` を開く。
別のターミナルで次を実行する。

```sh
npx playwright install chromium
npx playwright test tests/e2e/selection-catalog.spec.ts --workers=1
npm test
```

`CATALOG_BASE_URL` で検証サーバーを変更可能。静的JSON取得のため、HTMLファイルの直接オープンでは動作しない。外部サービスへの保存は行わない。ブラウザ保存が利用不可ならメモを書出して保管する。

## 再現状況の扱い

- 131要求・285受入条件を正本IRから取り込む。全件の再現確認は未完了。
- 部品名と要求ファミリーによる関連画像は発見の手掛かりだけであり、受入条件の達成証拠ではない。有料記事・インタビュー・BLPは「代表PoC検証あり・全条件未完了」。それ以外は引き続き未検証。
- 撮影ファイル名の末尾PC/SPだけが異なるものを同じ候補として束ねる。一方の撮影がない場合は明記し、別の型を代用しない。選択画面幅の記録がある候補を先に並べる。
- 有料記事・インタビュー・BLPの代表再現と標準/改善案を追加。非表示要求の操作・異常系デモ、全種別の追調査、全面のデザイン比較は残る。
- このカタログUIの改善だけで、全要求の再現やテーマ品質改善を完了としない。[実行計画](../../planning/2026-09-08-catalog-completion.md)を参照。

## 検証記録（2026-09-08）

Chromiumで検索、比較上限、PC/SP切替、要求・ACへの到達、保存後再読込、書出し・読込の往復、不正JSONの拒否と既存メモ保全、375pxの横溢れ、タブのキーボード移動、画像読込を検証。PC/SPスクリーンショットも目視確認。スクリーンショットはテストのローカル出力に生成する。要求整合性とHELIX consumer診断は `npm test` で別途確認する。
