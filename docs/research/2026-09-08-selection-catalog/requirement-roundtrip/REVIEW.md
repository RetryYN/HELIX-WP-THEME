# 候補と関連要求の往復

基準HEAD: 6806aafe629a4d7c1923c94822b6865e37d1c84b。Astra lowの実装・目視検収。

候補の詳細と比較欄から、要求ID・説明の冒頭・状態別件数を確認し、受入条件と残件へ移動できる。関連要求全体の記録であり候補画像の合否ではないことを明示する。説明全文は要求側に保持し、候補側は2行に収める。長い詳細でも閉じる操作を保持し、PCでは画像を追従させる。

遷移時は要求検索を対象IDにし、以前の証拠状態フィルタを解除する。元の候補へ戻る操作で検索・目的・選択状態フィルタ・要求フィルタ・比較3候補・PC/SP・倍率・スクロール位置を戻す。モーダルのDOMを保持するため、選択中のSP比較候補とメモ、画像内スクロールも維持する。起点の要求ボタンへフォーカスを戻す。

## 検証

- 既存22件＋新規3件のE2E: 25 passed。
- 新規は390/1440pxでキーボード往復、ゼロ件の要求検索からの遷移、フィルタ解除、比較3候補・SP・原寸倍率・検索・メモ保持、workspace JSONの往復前後一致を検査。
- 詳細の関連8要求とWT-FR-SP-01のmissing2条件を確認。関連なし負例はブラウザのレスポンス差替えで実施し、正本データを変更していない。
- 既存比較テストの広すぎるsection selectorは、追加された関連要求sectionを除くcompare-grid直下へ限定した。
- capture.mjsで390/1440pxの詳細・要求・復帰を6画像に記録。両幅overflowなし、復帰フォーカス一致。画像を目視確認。

## 範囲と限界

候補633件、要求133件、受入条件294件の正本は変更しない。テーマ・pluginsに変更なし。要求状態の昇格や未再現要求の実装完了ではない。

往復コンテキストは現在のタブ内の一時状態。ページ再読込後は既存workspace・メモ再開を使い、開いていたモーダルの往復ボタンまでは復元しない。
