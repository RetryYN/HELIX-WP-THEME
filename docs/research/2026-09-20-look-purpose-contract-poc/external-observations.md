# 外部・既存証跡観察

- WordPress の block style / theme.json による宣言境界は既存テーマの capability・style registry を正本として扱う。
- 本PoCは新しい見た目を実装せず、既存台帳と `results/verify.json` の実測を選択カタログへ再束縛する。
- 受入の意味は型数の多さではなく、用途台帳、PC/SP、JS無効、reduced-motion、コントラストの検査が同じ契約へ到達することに限定する。
