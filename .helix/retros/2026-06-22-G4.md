# Mini Retro: G4 (2026-06-22)

## Phase: 実装フェーズ (L3-L4)

## Keep
- G4 最小P0（carry無し）23/23 を達成。テスト計 101 全緑（unit+security 54 / integration 29 / E2E 18）。
- Wave3 で Playwright を新規導入し、apply-rollback / embed sandbox / consent gate を実WP(:8086)往復で E2E 検証。
- consent gate（L3-A4 §GAP-RT-022）を設計準拠で実装し、code-review APPROVE（Critical 0・漏洩経路なし明言）。
- 認証(WP cookie+X-WP-Nonce)を PM が curl で全ステップ実証→具体レシピ化したことで、繰返し失敗していた apply-rollback E2E が一発で通った。

## Problem
- apply-rollback E2E が3エージェント連続で未完了（WP login の testcookie 事前GET / admin/admin が実は不正 / 認証探索でのbudget枯渇）。
- サブエージェント最終報告が毎回途切れる（partial completion）→ Bash 直接検証が必須だった。
- dry-run 契約の罠（changes=配列・request_id=UUIDv4・dry-runとapplyで同一request_id）でテストが何度も400/412。
- consent gate・apply-rollback は test-plan 上は「テスト対象」だが実装が未/別経路で、テスト前に実装特定・機能実装が必要だった。

## Try
- @PM 2026-06-22 E2E 認証など環境依存の難所は、PM が先に curl で end-to-end 実証してから委譲する（探索コストをエージェントに払わせない）。
- @PM 2026-06-22 サブエージェントには「最低1回テスト実行してから報告」を明示し、報告後は必ず Bash で再検証する。

### メモ
- G4 スコープ外（carry依存で各機能Sprintへ繰延）: LLMO(037-041) / 移行SEO(056-060) / embed実装(066-073) / AI開示(061-064) / CLI(031-035)。
- PO裁定3件 OPEN: GAP-RT-044 / 046 / 047（docs/reviews/L3-PO-decision-packet.md）。
- 所見2件(VDD記録): TC-011 初回transientで /license/validate が grace 503 でなく gateway error(guard経由は正常・P2) / TC-048 noindex記事が crawl-map に robots付きで残存(test-planは除外想定・要design判断)。
- consent gate review Important I-1/I-2/I-3 は修正済。本番バナー選定 PERF-CARRY-002 は carry 据え置き。
