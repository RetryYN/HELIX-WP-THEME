import fs from 'node:fs';import {fixture,schema,scenarios,articles,structured,clock,validate} from '../docs/research/2026-09-20-product-registry-poc/model.mjs';import {allArticles,esc} from '../docs/research/2026-09-20-product-registry-poc/render.mjs';const root='docs/research/2026-09-20-product-registry-poc';validate(fixture);for(const [name,data]of [['product-schema',schema],['product-registry',fixture],['article-references',articles]])fs.writeFileSync(`${root}/${name}.json`,JSON.stringify(data,null,2)+'\n');
for(const s of scenarios)fs.writeFileSync(`${root}/${s.id}.html`,`<!doctype html><html lang="ja"><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><meta name="robots" content="noindex,nofollow"><title>${s.label}｜商品正本 PoC</title><link rel="stylesheet" href="registry.css"><body data-scenario="${s.id}"><a class="skip" href="#previews">記事へ進む</a><header class="site-header"><strong>HELIX / 商品正本</strong><span>架空データ・ローカル PoC</span></header><main><nav aria-label="候補">${scenarios.map(x=>`<a href="${x.id}.html" ${x.id===s.id?'aria-current="page"':''}>${x.label}</a>`).join('')}</nav><p class="eyebrow">PRODUCT REGISTRY / ONE SOURCE</p><h1>${s.label}</h1><p class="lead">${s.lead}</p><p class="boundary">3記事は商品IDだけを参照。編集値はページ内メモリに反映し、実保存・決済・カート・会員・外部APIを持ちません。2受入条件は部分確認。</p><section class="editor"><div><h2>商品正本を更新する</h2><p>名前・価格・特徴・評価・画像・リンク先はこの正本だけで管理。記事別の上書きは受け付けません。</p><noscript><p class="notice">JS無効でも3記事の初期表示と正本JSONを読めます。編集は動作しません。</p></noscript><label>schema付き商品JSON<textarea id="input" rows="9" spellcheck="false">${esc(JSON.stringify(fixture,null,2))}</textarea></label><div class="buttons"><button id="apply" disabled>検証して3記事へ反映</button><button id="reset" disabled>初期値に戻す</button></div><p id="status" role="status">未変更。再読込で初期値に戻ります。</p></div><aside><h2>参照と鮮度</h2><p>取得元は手動fixture。外部EC/ASPから取得していません。</p><p>検査時計：${clock}<br>各商品の取得時刻と確認期間から鮮度ラベルを表示。</p><p>期限超過は「要再確認」。実時間監視・自動再取得は未実装です。</p><details><summary>記事の参照だけを見る</summary><pre>${esc(JSON.stringify(articles,null,2))}</pre></details><p class="scope">affiliate用merchant feed / ProductGroupは要求対象外。実在レビュー・検索適格性は未検証。</p></aside></section><div id="previews" tabindex="-1">${allArticles(fixture,s.id)}</div><details class="output"><summary>構造化データの共通入力を見る</summary><pre id="structured-preview">${esc(JSON.stringify(articles.map(a=>structured(fixture,a)),null,2))}</pre></details><footer>WP 7.2実機・JSON/CPT永続保存・実記事への横断反映・権限/排他更新・外部APIは未接続。商品表示とJSON-LDの正本一致だけの代表PoC。AI・credentialなし。</footer></main><script type="module" src="view.mjs"></script></body></html>\n`);
